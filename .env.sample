# === Local file persistence ===
DATA_DIR=./data

# === OpenAI (optional for auto-categorization & metadata extraction) ===
OPENAI_API_KEY=

# === Google Drive (optional for cloud backup) ===
# Choose either OAuth user credentials or a Service Account.
# For OAuth user flow:
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/api/google/oauth2callback
GOOGLE_REFRESH_TOKEN=

# For Service Account (upload to a specific Drive or shared drive):
GOOGLE_SERVICE_ACCOUNT_JSON= # JSON string or path to json file
GOOGLE_DRIVE_PARENT_FOLDER_ID= # Optional: the folder id to upload files into